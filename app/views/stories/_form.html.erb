<%= form_with(model: story, local: true) do |form| %>
  <% if story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(story.errors.count, "error") %> prohibited this story from being saved:</h2>

      <ul>
      <% story.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :from %>
    <%= form.text_field :from, id: :story_from %>
  </div>

  <div class="field">
    <%= form.label :for %>
    <%= form.text_field :for, id: :story_for %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :story_type %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :story_price %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :story_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :story_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
